# PostgreSQL Connection (Railway fornece isso automaticamente)
DATABASE_URL=postgresql://postgres:password@localhost:5432/thirdeye?schema=public

# Ambiente
NODE_ENV=production

# CORS - URL do frontend na Vercel
CORS_ORIGIN=https://seu-projeto.vercel.app

# Porta (Railway injeta automaticamente; apenas para local)
PORT=3000

